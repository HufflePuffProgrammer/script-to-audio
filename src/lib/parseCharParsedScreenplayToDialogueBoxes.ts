export function parseCharParsedScreenplayToDialogueBoxes(CPResults: string, PSResults: string){
    console.log("Parsing character parsed screenplay to dialogue boxes:", CPResults, PSResults);

        
    return { dialogueBoxes: [], error: null};
}